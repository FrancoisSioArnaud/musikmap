Read `AGENTS.md` and the local `AGENTS.md` files relevant to this task first.

Do not code immediately.

First:
1. restate the relevant constraints from the workspace,
2. list the files to inspect,
3. state whether SCSS changes are required,
4. propose a minimal implementation plan.

Do not modify SCSS unless I explicitly authorize it.
If SCSS changes are required to complete the task, stop and ask first.

Once the plan is clear, implement the feature with the smallest safe patch.
Do not refactor unrelated code.
Do not add dependencies unless explicitly authorized.

After the patch, run the smallest relevant validation commands.

Return:
1. Summary
2. Files changed
3. Commands run
4. Remaining risks or assumptions
